name: Appium CICD Try

On:
  workflow_dispatch:

jobs:
  run_first_appium_test:
    runs-on: ubuntu-latest


    steps:
    - name: pull the code first
      uses: actions/checkout@v3

    - name: Set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: maven

    - name: Set up node
     
